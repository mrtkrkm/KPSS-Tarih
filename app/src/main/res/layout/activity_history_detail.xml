<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:background="@color/background_gray"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context="muratk.mktarih.Activities.HistoryDetailActivity">
    <android.support.v7.widget.Toolbar
        android:id="@+id/tb_main"
        android:layout_height="?attr/actionBarSize"
        android:layout_width="match_parent"
        android:minHeight="?attr/actionBarSize"
        android:layout_alignParentLeft="true"
        android:elevation="4dp"
        app:popupTheme="@style/ThemeOverlay.AppCompat"
        android:background="@color/dark_blue_gray_800"/>

    <ListView
        android:layout_below="@+id/tb_main"
        android:id="@+id/listview_history_detail"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginLeft="5dp"
        android:layout_marginRight="5dp"
        android:divider="@null"
        android:scrollbars="none">
    </ListView>

</RelativeLayout>
